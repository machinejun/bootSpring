// Spring boot --> 내장 톰캣을 가지고 있다.
// 아파치 + 톰캣(java 코드를 컴파일 하기위해서는 tomcat이 필요하다

)
// 아파치 : 웹서버
// spring boot --> url 요청할수 없다
               --> uri 방식으로 요청 --> 아파치 --> 톰캣 동작
               --> 블로그 게시물 100개 (.html 개를 만들어 놓아야함)
               --> 동적인 파일을 생성할 수 있다면 ( 템플릿 : index.jsp )